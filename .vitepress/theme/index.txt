import DefaultTheme from "vitepress/theme";
import Popup from "./popup.js";
import "./popup.css";

export default {
  ...DefaultTheme,
  enhanceApp({ app }) {
    if (typeof window !== "undefined") {
      Popup.mounted();
    }
  }
};
// index.js
